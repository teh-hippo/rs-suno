{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "description": "Top-level configuration parsed from a TOML file.",
  "type": "object",
  "properties": {
    "accounts": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/AccountConfig"
      }
    },
    "defaults": {
      "$ref": "#/$defs/Defaults"
    }
  },
  "$defs": {
    "AccountConfig": {
      "description": "Configuration for a single named account.",
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/$defs/AudioFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "account_id": {
          "description": "Optional Suno user id to assert this account authenticates as, refusing\n to run on a mismatch (a belt-and-braces check alongside the on-disk\n owner pin in the lineage store).",
          "type": [
            "string",
            "null"
          ]
        },
        "albums": {
          "description": "Manual album-name overrides, keyed by the album's stable lineage root id\n (`<root_id> = \"Preferred Name\"`). Account-wide, never per-source, since\n album identity is the lineage root. An empty or whitespace-only value is\n ignored, so a stray key cannot blank an album.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "animated_cover_compression_level": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_cover_lossless": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "animated_cover_max_fps": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_max_width": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_quality": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_covers": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "areas": {
          "description": "Per-area mode selection (`sync` vs `copy`) for this account's library,\n liked feed, and playlists. Absent means the classic single-verb run.",
          "anyOf": [
            {
              "$ref": "#/$defs/AreasConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "character_set": {
          "anyOf": [
            {
              "$ref": "#/$defs/CharacterSet"
            },
            {
              "type": "null"
            }
          ]
        },
        "concurrency": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "details_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "download_stems": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lead_tracks": {
          "description": "Clip ids (or unique id prefixes, e.g. the 8-char code from a filename)\n flagged as their lineage album's lead: each is promoted to track 1,\n shifting the rest down. Account-wide; a clip's album is inferred from its\n resolved root, so the album is never named here.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "lrc_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lyrics_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "min_newest": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "naming_template": {
          "type": [
            "string",
            "null"
          ]
        },
        "number_singletons": {
          "description": "Whether a single-track (lone) lineage album is given a track number. When\n unset it defaults to `true`; `false` leaves singletons unnumbered so a\n `{track2}` prefix does not decorate a standalone song.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "retries": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "root": {
          "type": [
            "string",
            "null"
          ]
        },
        "sources": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/SourceConfig"
          }
        },
        "stem_format": {
          "anyOf": [
            {
              "$ref": "#/$defs/StemFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "token": {
          "type": [
            "string",
            "null"
          ]
        },
        "token_command": {
          "description": "The command whose stdout mints a token. Resolved from the\n `SUNO_[<LABEL>_]TOKEN_COMMAND` env tiers then the config keys. There is\n deliberately no `--token-command` flag, so it is never read from\n [`FlagOverrides`]; set it in config or the environment.",
          "type": [
            "string",
            "null"
          ]
        },
        "video_cover_retention": {
          "anyOf": [
            {
              "$ref": "#/$defs/VideoCoverRetention"
            },
            {
              "type": "null"
            }
          ]
        },
        "video_mp4": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "AreaMode": {
      "description": "Deletion mode for the library area: 'off' arms deletion of library-exclusive files, 'copy' is additive, 'mirror' deletes.",
      "type": "string",
      "enum": [
        "off",
        "copy",
        "mirror"
      ]
    },
    "AreasConfig": {
      "description": "Per-area mode selection for an account.\n\n `library` accepts `off`/`copy`/`mirror`; `liked` and `playlists` accept\n `copy`/`mirror`; `playlist` overrides individual playlists by Suno id.\n `deny_unknown_fields` turns a mistyped key into a parse error rather than a\n silent no-op; the `playlist` map's dynamic ids can't use it, but its closed\n [`SourceMode`] values still reject a bad mode at parse time.",
      "type": "object",
      "properties": {
        "library": {
          "anyOf": [
            {
              "$ref": "#/$defs/AreaMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "liked": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "playlist": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/SourceMode"
          },
          "default": {}
        },
        "playlists": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceMode"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "AudioFormat": {
      "description": "Audio format for downloaded clips.",
      "type": "string",
      "enum": [
        "mp3",
        "flac",
        "wav",
        "alac"
      ]
    },
    "CharacterSet": {
      "type": "string",
      "enum": [
        "unicode",
        "ascii"
      ]
    },
    "Defaults": {
      "description": "Global default settings applied when no account or source override applies.",
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/$defs/AudioFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "animated_cover_compression_level": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_cover_lossless": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "animated_cover_max_fps": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_max_width": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_quality": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_covers": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "character_set": {
          "anyOf": [
            {
              "$ref": "#/$defs/CharacterSet"
            },
            {
              "type": "null"
            }
          ]
        },
        "concurrency": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "details_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "download_stems": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lrc_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lyrics_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "min_newest": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "naming_template": {
          "type": [
            "string",
            "null"
          ]
        },
        "number_singletons": {
          "description": "Whether a single-track (lone) lineage album is given a track number. When\n unset it defaults to `true`; `false` leaves singletons unnumbered so a\n `{track2}` prefix does not decorate a standalone song.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "retries": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "stem_format": {
          "anyOf": [
            {
              "$ref": "#/$defs/StemFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "token_command": {
          "description": "The command whose stdout mints a token. Resolved from the\n `SUNO_[<LABEL>_]TOKEN_COMMAND` env tiers then the config keys. There is\n deliberately no `--token-command` flag, so it is never read from\n [`FlagOverrides`]; set it in config or the environment.",
          "type": [
            "string",
            "null"
          ]
        },
        "video_cover_retention": {
          "anyOf": [
            {
              "$ref": "#/$defs/VideoCoverRetention"
            },
            {
              "type": "null"
            }
          ]
        },
        "video_mp4": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "SourceConfig": {
      "description": "Per-source overridable settings within an account.",
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/$defs/AudioFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "animated_cover_compression_level": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_cover_lossless": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "animated_cover_max_fps": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_max_width": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "animated_cover_quality": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "maximum": 255,
          "minimum": 0
        },
        "animated_covers": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "character_set": {
          "anyOf": [
            {
              "$ref": "#/$defs/CharacterSet"
            },
            {
              "type": "null"
            }
          ]
        },
        "concurrency": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "details_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "download_stems": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lrc_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "lyrics_sidecar": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "min_newest": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "naming_template": {
          "type": [
            "string",
            "null"
          ]
        },
        "number_singletons": {
          "description": "Whether a single-track (lone) lineage album is given a track number. When\n unset it defaults to `true`; `false` leaves singletons unnumbered so a\n `{track2}` prefix does not decorate a standalone song.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "retries": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "stem_format": {
          "anyOf": [
            {
              "$ref": "#/$defs/StemFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "token_command": {
          "description": "The command whose stdout mints a token. Resolved from the\n `SUNO_[<LABEL>_]TOKEN_COMMAND` env tiers then the config keys. There is\n deliberately no `--token-command` flag, so it is never read from\n [`FlagOverrides`]; set it in config or the environment.",
          "type": [
            "string",
            "null"
          ]
        },
        "video_cover_retention": {
          "anyOf": [
            {
              "$ref": "#/$defs/VideoCoverRetention"
            },
            {
              "type": "null"
            }
          ]
        },
        "video_mp4": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "SourceMode": {
      "description": "How a selected source treats its clips: mirror with deletion, or additive copy.",
      "oneOf": [
        {
          "description": "Mirror the source, deleting local files that leave it (rclone `sync`).",
          "type": "string",
          "const": "mirror"
        },
        {
          "description": "Copy additively; never delete (rclone `copy`).",
          "type": "string",
          "const": "copy"
        }
      ]
    },
    "StemFormat": {
      "description": "Container format for a downloaded stem.\n\n Stems are stored RAW in their native container and are never transcoded, so\n unlike [`AudioFormat`] there is no lossless-from-lossy render: WAV comes\n straight from Suno's free `convert_wav` endpoint and MP3 straight from the\n public CDN. FLAC is deliberately unrepresentable — a stem is never\n re-encoded to FLAC, even when the song's own format is FLAC.",
      "oneOf": [
        {
          "description": "Lossless WAV via the free `convert_wav` render, stored as delivered.",
          "type": "string",
          "const": "wav"
        },
        {
          "description": "The public CDN MP3, stored as delivered.",
          "type": "string",
          "const": "mp3"
        }
      ]
    },
    "VideoCoverRetention": {
      "description": "Which video-cover artifacts to retain.",
      "type": "string",
      "enum": [
        "neither",
        "webp",
        "mp4",
        "both"
      ]
    }
  }
}
